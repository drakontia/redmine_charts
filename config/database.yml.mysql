mysql: &mysql
  adapter: mysql2
  username: root
  password:
  database: charts

defaults: &defaults
  adapter: mysql2
  pool: 5
  timeout: 5000
  <<: *<%= ENV['DB'] || "unspecified" %>

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
